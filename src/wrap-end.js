// (function(root) {
//   root.patterns=require("patterns");
//   root.patterns.Parser=require(import Parser from "../../core/parser";);
//   root.patterns.logging=require(import logger from "../../core/logger";);
//   root.patterns.utils=require(import utils from "../../core/utils";);
//   root.patterns.logger=require(import logger from "../../core/logger";);
//   root.patterns.Base=require(import Base from "../../core/base";);
// })(window);
// window.define=undefined;
