/* pat-clone */
define([
    "jquery",
    "pat-registry",
    "pat-base"
], function($, registry, Base) {

    return Base.extend({
        name: "clone",
        trigger: ".pat-clone",

        init: function($el, options) {
            debugger;
        },
    });
});
// vim: sw=4 expandtab
